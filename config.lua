Config = {}

-- Identifier used for ownership
Config.OwnerIdentifier = 'license' 
-- alternatives: 'discord', 'steam'

-- Command permissions (This is who will be able to set ownership of vehicles without owning it)
Config.AdminAce = 'vehicle.admin'

-- DiscordWebhook
Config.DiscordWebhook = 'https://discord.com/api/webhooks/XXXXXXXXXX'
Config.ServerName = 'Server Name'

-- Commands can be changed at the bottom of client.lua (vehmenu and vehadmin are the ones already)